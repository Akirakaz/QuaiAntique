<!DOCTYPE html>

<title>Edit Booking</title>

{% block body %}
    <h1>Edit Booking</h1>

    {{ include('public/booking/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_booking') }}">back to list</a>

    {{ include('public/booking/_delete_form.html.twig') }}
{% endblock %}
